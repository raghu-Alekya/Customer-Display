
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

subprojects {
    if (it.name != 'app') {
        evaluationDependsOn(':app')
    }
}

tasks.register("clean", Delete) {
    delete rootProject.layout.buildDirectory
}
